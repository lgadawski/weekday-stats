package com.gadawski;

rule "Home automation rule"
    when
        //timer (cron:* 0/60 * * * ?)
        timer (cron:0/5 * * * * ?)
        $o: Object()
        // presence
    then
        System.out.println("Rule fired for object: " + $o.toString());
end